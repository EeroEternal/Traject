# Phase D e2e on pro6000 (8× RTX PRO 6000)

Host: `bodesi@39.183.171.3:2208` (`pro6000`)

## Environment

| Item | Path / value |
|------|----------------|
| Traject tree | `~/Traject` |
| Rust | `~/.cargo` (1.97.1) |
| Venv | `/home/bodesi/venvs/sglang-lite` |
| HF model | `/home/bodesi/models/ds-v4-flash` → DeepSeek-V4-Flash-0731 |
| Converted TP=8 | `/home/bodesi/models/ds-v4-mp8` |
| Engine | sglang-lite `:9001` (`third_party` or `/home/bodesi/project/sglang-lite`) |

Missing deps fixed on host: `fastapi`, `uvicorn` (venv previously lacked process server deps).

## Start

```bash
source ~/.cargo/env
cd ~/Traject
export SGLANG_VENV=/home/bodesi/venvs/sglang-lite
export SGLANG_LITE_DSV4_HF=/home/bodesi/models/ds-v4-flash
export SGLANG_LITE_DSV4_CONVERTED=/home/bodesi/models/ds-v4-mp8
export SGLANG_LITE_ROOT=/home/bodesi/project/sglang-lite   # or vendored third_party
bash scripts/start_engine.sh
cargo build -p traject-cli --release
```

## Results (2026-08-06)

### Simple Generate

```text
prompt: 用一句话介绍你自己
answer: 我是DeepSeek，一个由深度求索公司打造的AI助手…
trajectory Finished, steps=1, memory_nodes=3
path: Driver → Scheduler → SglangLiteEngineBackend → :9001
```

### Zene agent (`scripts/e2e_agent_5090.sh`)

```text
trajectory=6fcddd8b-...
generate_steps=3
tool_steps=2
history_len=5
path: driver/scheduler for every Generate + Tool
answer: hello.txt 的内容是 "hello from traject+zene"…
```

Logs contain:

- `external trajectory (agent-driven)`
- `generate step via driver/scheduler` (×3)
- `tool step via driver/scheduler` (×2)
- `sglang-lite generate finished` with `trajectory` / `step` ids
- `cache_hit_tokens` field present (values 0 on cold / V4 path — physical prefix hit still engine-side work)

### GPU

~25 GiB used per GPU after load (8 ranks).

## Follow-up optimizations (post Phase D) — verified 2026-08-06 05:17

Shipped + re-validated on pro6000:

1. **Tool latency → pin TTL** — logs show `p95_ms=Some(6)` on Glob tool steps.
2. **Prefetch pin** — engine log: `reason=Prefetch` after tools.
3. **Engine prefix pin API** — `prefix pin id=… refs=N reason=WaitingTool|Prefetch session=…`.
4. **Session LCP** — engine log: `prompt_lcp=2442 prev_len=2444 new_len=2492` (multi-turn extension).
5. **V4 logits coerce** — handle Tensor | ModelOutput | tuple (largest-last-dim logits).
6. Agent e2e again: `generate_steps=3 tool_steps=2 history_len=5`, correct Chinese summary.

Still open:

1. Physical GPU free when MemoryManager evicts a node (engine pin is soft bookkeeping).
2. Full in-process weight runner.
3. Promote `prompt_lcp` into `cache_hit_tokens` when V4 snapshot miss but token LCP is high (optional metric).
