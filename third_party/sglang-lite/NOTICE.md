# sglang-lite (vendored)

Source: https://github.com/ParaTensor/sglang-lite (Apache-2.0)

This tree is Traject's first-party copy of the MoE token factory:

- `engine/` — Python GPU runner (`sglang_lite` package via `pyproject.toml`)
- `control/` / `serving/` — Rust OpenAI control plane (workspace members of Traject)

Do not treat `~/project/sglang-lite` as the integration source; edit here.
